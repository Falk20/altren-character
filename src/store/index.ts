import { createStore } from "vuex";

import character from "./modules/character";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    character,
  },
});
